[project]
name = "django-stripe"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "django>=5.2.8",
    "gunicorn>=23.0.0",
    "psycopg[binary]>=3.2.12",
    "python-decouple>=3.8",
    "stripe>=14.0.0",
]
